# Plan: <project>

Local (not committed). Produced and maintained by `/spec`. Consumed by `/orchestrate`.
Structure: see `schemas/plan.md`. Acceptance criteria must be checkable by the reviewer.

## Overview
<2-4 lines: goal, who it is for, what "done" looks like>

## Branch scheme
- main — merge target; holds committed code + `DEVLOG.md`.
- <ordered feature branches and how they relate>

## Features

### <feature>   (branch: feature_<name>, depends_on: —)
Spec:
- ...

Acceptance criteria (reviewer checks these):
- [ ] ...

Non-goals:
- ...

Constraints:
- ...

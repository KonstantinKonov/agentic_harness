# Скопировать в .env и заполнить. .env не коммитим.
VPS_HOST=user@1.2.3.4          # SSH-доступ по ключу, без пароля
DEMO_DOMAIN=demo.example.ru    # A-запись уже указывает на IP VPS
ANTHROPIC_API_KEY=sk-ant-...   # если demo-сервис ходит в LLM
